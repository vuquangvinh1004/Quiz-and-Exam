name: CI

on:
  push:
    branches: ["main", "develop", "feature/**"]
  pull_request:
    branches: ["main", "develop"]

jobs:
  test:
    name: Test (Python ${{ matrix.python-version }})
    runs-on: windows-latest          # app is Windows-only (installer, paths)

    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12", "3.13"]

    steps:
      - uses: actions/checkout@v5

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v6
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install -r requirements-dev.txt

      - name: Run unit tests with coverage
        env:
          QT_QPA_PLATFORM: offscreen
        run: |
          python -m pytest tests/unit/ tests/integration/ -q --tb=short `
            --cov=core --cov=modules `
            --cov-report=term-missing `
            --cov-fail-under=80

      - name: Run UI smoke tests
        env:
          QT_QPA_PLATFORM: offscreen
        run: |
          python -m pytest tests/ui/ -q --tb=short

  lint:
    name: Lint
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.13"
          cache: pip

      - name: Install ruff
        run: pip install ruff

      - name: Check lint (high-signal)
        run: ruff check . --output-format=github --select E9,F63,F7,F82,F821
